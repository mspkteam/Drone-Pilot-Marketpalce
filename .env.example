# Neon PostgreSQL (required)
# Pooled — app runtime (hostname includes -pooler)
DATABASE_URL="postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/neondb?sslmode=require"
# Direct — Prisma CLI (db push, migrate, studio); no -pooler in host
# Vercel/Neon often names this DATABASE_URL_UNPOOLED — we accept either:
DIRECT_URL="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"
# DATABASE_URL_UNPOOLED="postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require"

# Auth.js — generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-random-secret-at-least-32-characters"

# Vercel / production: set AUTH_URL to your deployment URL
# AUTH_URL="https://your-app.vercel.app"

# Email: leave SMTP_URL unset — demo logs to console only (no real SMTP)
# SMTP_URL=
# EMAIL_FROM=noreply@dronepilot.local

# Payments: no Stripe keys — marketplace and shop use internal demo pay only
